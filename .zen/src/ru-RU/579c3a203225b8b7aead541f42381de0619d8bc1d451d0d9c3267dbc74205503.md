---
'title': 'Тестирование отображения формул KaTeX'
'summary': 'Этот документ представляет собой пример для тестирования отображения формул KaTeX, содержащий несколько встроенных и блочных математических формул, таких как E=mc^2, интегралы, производные, ряды, кусочные функции, таблицы, матрицы и т.д., с целью проверки корректного рендеринга формул.'
'tags':
  - 'KaTeX'
  - 'тестирование формул'
  - 'математические формулы'
  - 'LaTeX'
  - 'отображение'
  - 'пример документа'
'inferred_lang': 'ru-RU'
---

# Тестирование отображения формул KaTeX

Это тест встроенной формулы: $ E = mc^2 $.

Это тест блочной формулы:

$$
\int_a^b f(x) \, dx = F(b) - F(a)
$$

Это ещё один тест блочной формулы:

$$
\frac{d}{dx}e^x = e^x
$$

Это тест сложной формулы:

$$
\sum_{n=1}^{\infty} \frac{1}{n^2} = \frac{\pi^2}{6}
$$

Тест формулы с конструкцией cases:

$$
f(x) =
\begin{cases}
x^2 & \text{if } x \geq 0 \\
-x & \text{if } x < 0
\end{cases}
$$

Формула с таблицей:

$$
\begin{array}{c|c|c}
a & b & c \\
\hline
1 & 2 & 3 \\
4 & 5 & 6 \\
\end{array}
$$

Формула с матрицей:

$$
\begin{pmatrix}
1 & 2 \\
3 & 4
\end{pmatrix}
$$

Тест формулы с текстом:

$$
E = mc^2 \quad \text{где } m \text{ — масса, а } c \text{ — скорость света}
$$

Обобщённая формула Келли:

$$
f^* = \argmax_f \sum_{i \in X} P_i \ln (1 + f \cdot R_i)
$$

Пожалуйста, убедитесь, что все вышеприведённые формулы отображаются корректно.
