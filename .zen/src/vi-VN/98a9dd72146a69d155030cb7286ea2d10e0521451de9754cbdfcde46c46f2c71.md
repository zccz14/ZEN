---
'title': 'ZEN - Trình tạo trang tĩnh Markdown + AI theo chủ nghĩa tối giản'
'summary': 'ZEN là trình tạo trang tĩnh tích hợp AI, được thiết kế dành riêng cho tài liệu Markdown. Nó cho phép người dùng chuyển đổi thư mục Git chứa file Markdown thành trang HTML tĩnh mà không cần cấu hình phức tạp, tất cả cài đặt được ẩn trong thư mục .zen. Chức năng chính bao gồm điều hướng phân loại thông minh (sử dụng AI phân tích nội dung để tạo sơ đồ trang), dịch tự động tăng dần (hỗ trợ phiên bản đa ngôn ngữ, người dùng có thể viết bằng ngôn ngữ mẹ đẻ, AI xử lý dịch thuật), và trải nghiệm viết tối thiểu gây gián đoạn. Hướng dẫn bắt đầu nhanh yêu cầu cài đặt Node.js, lấy OpenAI API Key và Git, xây dựng trang qua dòng lệnh, hỗ trợ chỉ định ngôn ngữ và URL cơ sở.'
'tags':
  - 'Trình tạo trang tĩnh'
  - 'Markdown'
  - 'AI'
  - 'Dịch đa ngôn ngữ'
  - 'Chủ nghĩa tối giản'
  - 'Công cụ tài liệu'
  - 'Git'
'inferred_lang': 'zh-Hans'
---

# ZEN - Trình tạo trang tĩnh Markdown + AI theo chủ nghĩa tối giản

[> Xem Demo Trang Web](https://zen.zccz14.com/)

## Chức năng cơ bản

1. 📃 **Tạo trang tĩnh**: Biến bất kỳ thư mục git chứa Markdown nào thành một trang HTML tĩnh.
2. 🧭 **Điều hướng phân loại thông minh**: Sử dụng AI phân tích nội dung, tạo sơ đồ trang và điều hướng, không phụ thuộc vào vị trí file nguồn.
3. 🌍 **Dịch tự động tăng dần**: Sử dụng AI để dịch tăng dần, cho phép người dùng viết Markdown bằng ngôn ngữ mẹ đẻ, nhưng người dùng có thể đa ngôn ngữ.

Tại sao lại tạo thêm một trình tạo trang tĩnh Markdown nữa? Tôi đã suy nghĩ lại về cách công cụ tài liệu có thể phục vụ người viết tốt hơn trong thời đại AI:

- Trở về với nội dung: Tối thiểu hóa sự gián đoạn, tập trung vào việc viết

  Đưa tài liệu trở lại bản chất, đưa việc viết trở lại sự tĩnh lặng.

  Thứ nhất, tôi không muốn làm thủ thư. Đôi khi tôi muốn viết một số nội dung, nhưng có thể tôi chưa nghĩ ra tiêu đề cho nó, chưa hình thành cấu trúc tổ chức cụ thể. Tôi hy vọng LLM sẽ tự động giúp tôi tạo tiêu đề, tóm tắt, phân loại, điều hướng và các công việc sắp xếp khác.

  Thứ hai, thư phòng của tôi phải được dọn dẹp sạch sẽ. Tôi không muốn các công cụ xây dựng cấu hình phức tạp, mất công với cấu hình tài liệu phức tạp, không thích cấu trúc rườm rà. Mỗi lần nhìn thấy file cấu hình và cấu trúc dự án của các công cụ như Docusaurus, VuePress, Astro, tôi lại đau đầu. Giờ đây, ZEN chọn cách ẩn tất cả cấu hình vào thư mục .zen, quét vào góc, người dùng chỉ cần tập trung vào nội dung viết, phần còn lại để ZEN xử lý. Các thư mục khác là không gian viết của bạn, bạn có thể tổ chức file Markdown tùy ý.

- Trở về với tiếng mẹ đẻ: Viết bằng tiếng mẹ đẻ, xây dựng phiên bản đa ngôn ngữ mượt mà

  Đời người ngắn ngủi, tôi lười dịch. Khi sử dụng tiếng mẹ đẻ, con người mới có thể phát huy tối đa chiều sâu tư duy của mình. Nhưng đồng thời việc kết nối với thế giới là cần thiết, tôi không muốn từ bỏ giao tiếp đa ngôn ngữ. Vì vậy, tôi hy vọng có một công cụ cho phép tôi viết bằng tiếng mẹ đẻ, đồng thời lại để nhiều người hơn thấy được nội dung của tôi. Hiện tại, i18n không chỉ yêu cầu tự dịch mà còn phải tự duy trì cập nhật nội dung, quá phiền phức. Tôi chọn sử dụng LLM để giúp tôi dịch, tiết kiệm rất nhiều thời gian và có thể hoàn thành dịch đa ngôn ngữ chỉ với một cú nhấp chuột.

## Bắt đầu nhanh

Điều kiện tiên quyết:

- Đã cài đặt [Node.js](https://nodejs.org/) (khuyến nghị phiên bản 18 trở lên, cần có lệnh npx)
- Đã lấy [OpenAI API Key](https://platform.openai.com/account/api-keys), hoặc API Key tương thích OPENAI
- Đã cài đặt Git (dùng để liệt kê file Markdown từ Git, bỏ qua các file bị lọc bởi quy tắc .gitignore)

Đầu tiên, làm việc trong một thư mục đã được quản lý bởi git:

Cấu hình biến môi trường

```bash
export OPENAI_API_KEY="sk-xxxxxx"  # Bắt buộc: Vui lòng thay thế bằng OpenAI API Key của bạn
export OPENAI_BASE_URL="https://api.openai.com/v1"  # Tùy chọn, mặc định sử dụng API chính thức của OpenAI
export OPENAI_MODEL="gpt-3.5-turbo"  # Tùy chọn, mặc định sử dụng mô hình gpt-3.5-turbo
```

Xây dựng trang, hỗ trợ dịch đa ngôn ngữ. Sử dụng thư mục hiện tại làm thư mục nguồn, xuất ra thư mục `.zen/dist`.

- Có thể chỉ định phiên bản ngôn ngữ cần tạo bằng cách sử dụng nhiều lần tham số `--lang` (ví dụ: `--lang zh-Hans --lang en-US`).
- Có thể chỉ định tiền tố URL cơ sở của trang bằng tham số `--base-url` (ví dụ: tên kho lưu trữ trên GitHub Pages).
- Không cần cấu hình ngôn ngữ nguồn, ZEN sẽ tự động phát hiện.

```bash
npx zengen@latest build --lang zh-Hans --lang en-US
```

**Xem thêm tham số hoặc trợ giúp**:

```bash
npx zengen@latest
```
