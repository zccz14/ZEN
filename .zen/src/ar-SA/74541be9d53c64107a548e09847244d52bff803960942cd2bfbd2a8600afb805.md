---
'title': 'ZEN - منشئ المواقع الثابتة البسيط باستخدام Markdown + الذكاء الاصطناعي'
'summary': 'ZEN هو منشئ مواقع ثابتة مزود بوظائف الذكاء الاصطناعي، يهدف إلى تبسيط عملية كتابة المستندات وإنشاء المواقع. يسمح للمستخدمين بالتركيز على كتابة محتوى Markdown بلغتهم الأم، ويتعامل تلقائياً مع إنشاء العناوين، والتنقل عبر التصنيفات، والترجمة التدريجية، ويدعم إصدارات متعددة اللغات دون الحاجة إلى إعدادات معقدة. يعتمد الأداة على Node.js وواجهة برمجة تطبيقات OpenAI، ويبني المواقع بسرعة عبر سطر الأوامر، مما يجعله مناسباً للكتاب الذين يبحثون عن الكفاءة والبساطة.'
'tags':
  - 'منشئ المواقع الثابتة'
  - 'Markdown'
  - 'الذكاء الاصطناعي'
  - 'البساطة'
  - 'الترجمة متعددة اللغات'
  - 'Node.js'
  - 'OpenAI'
'inferred_lang': 'zh-Hans'
---

# ZEN - منشئ المواقع الثابتة البسيط باستخدام Markdown + الذكاء الاصطناعي

[> عرض تجريبي للموقع](https://zccz14.github.io/ZEN/)

## الوظائف الأساسية

1. 📃 **إنشاء موقع ثابت**: تحويل أي مجلد git يحتوي على Markdown إلى موقع HTML ثابت.
2. 🧭 **التنقل الذكي عبر التصنيفات**: استخدام الذكاء الاصطناعي لتحليل المحتوى، وإنشاء خريطة الموقع والتنقل، دون الحساسية لموقع الملفات المصدر.
3. 🌍 **الترجمة التلقائية التدريجية**: استخدام الذكاء الاصطناعي للترجمة التدريجية، مما يسمح للمستخدمين بكتابة Markdown بلغتهم الأم، مع دعمهم بلغات متعددة.

لماذا قمت بإنشاء منشئ مواقع ثابت آخر باستخدام Markdown؟ لقد فكرت مرة أخرى في كيفية خدمة أدوات المستندات للكتاب بشكل أفضل في عصر الذكاء الاصطناعي:

- العودة إلى المحتوى: تقليل الإزعاج، والتركيز على الكتابة

  إعادة المستندات إلى جوهرها، وإعادة الهدوء إلى الكتابة.

  أولاً، لا أريد أن أكون أمين مكتبة. أحياناً أريد كتابة بعض المحتوى، لكنني قد لا أكون قد فكرت في عنوانه بعد، أو لم أقم بتنظيمه بشكل خاص. أتمنى أن يقوم LLM تلقائياً بإنشاء العناوين، والملخصات، والتصنيفات، والتنقل، وغيرها من أعمال التنظيم.

  ثانياً، يجب أن يكون مكتبي نظيفاً. لا أريد أدوات بناء معقدة الإعدادات، أو التعامل مع إعدادات المستندات المعقدة، ولا أحب الهياكل المعقدة. كلما رأيت ملفات الإعدادات وهياكل المشاريع الخاصة بأدوات مثل Docusaurus وVuePress وAstro، أشعر بالصداع. الآن، اختار ZEN إخفاء جميع الإعدادات في مجلد `.zen`، ودفعها إلى الزاوية، بحيث يركز المستخدم فقط على محتوى الكتابة، ويتولى ZEN الباقي. جميع المجلدات الأخرى هي مساحة الكتابة الخاصة بك، ويمكنك تنظيم ملفات Markdown الخاصة بك كما تشاء.

- العودة إلى اللغة الأم: الكتابة بلغتك الأم، وإنشاء إصدارات متعددة اللغات بسلاسة

  الحياة قصيرة، وأنا كسول للترجمة. عندما يستخدم الإنسان لغته الأم، يكون قادراً على استغلال عمق تفكيره بشكل أفضل. ولكن في نفس الوقت، من الضروري الحفاظ على الاتصال بالعالم، ولا أريد التخلي عن التواصل متعدد اللغات. لذلك، أتمنى أن تكون هناك أداة تسمح لي بالكتابة بلغتي الأم، وفي نفس الوقت تسمح للمزيد من الأشخاص برؤية محتواي. حالياً، تتطلب i18n الترجمة الذاتية بالإضافة إلى الحفاظ على تحديث المحتوى، وهذا أمر مزعج للغاية. اخترت استخدام LLM لمساعدتي في الترجمة، مما وفر علي الكثير من الوقت، ويمكن إكمال ترجمة لغات متعددة بنقرة واحدة.

## البدء السريع

المتطلبات المسبقة:

- تثبيت [Node.js](https://nodejs.org/) (يُفضل الإصدار 18 أو أعلى، مع وجود أمر npx)
- الحصول على [مفتاح واجهة برمجة تطبيقات OpenAI](https://platform.openai.com/account/api-keys)، أو مفتاح واجهة برمجة تطبيقات متوافق مع OPENAI
- تثبيت Git (لإدراج ملفات Markdown من Git، وتجاهل الملفات التي تمت تصفيتها بواسطة قواعد .gitignore)

أولاً، اعمل داخل مجلد تمت إدارته بواسطة Git:

تكوين متغيرات البيئة

```bash
export OPENAI_API_KEY="sk-xxxxxx"  # إلزامي: يرجى استبداله بمفتاح واجهة برمجة تطبيقات OpenAI الخاص بك
export OPENAI_BASE_URL="https://api.openai.com/v1"  # اختياري، يستخدم واجهة برمجة تطبيقات OpenAI الرسمية افتراضياً
export OPENAI_MODEL="gpt-3.5-turbo"  # اختياري، يستخدم نموذج gpt-3.5-turbo افتراضياً
```

بناء الموقع، مع دعم الترجمة متعددة اللغات. استخدم المجلد الحالي كمجلد مصدر، وأخرج إلى مجلد `.zen/dist`.

- يمكنك تحديد إصدارات اللغة المطلوبة عن طريق استخدام معلمة `--lang` عدة مرات (مثل `--lang zh-Hans --lang en-US`).
- يمكنك تحديد بادئة عنوان URL الأساسي للموقع باستخدام معلمة `--base-url` (مثل اسم المستودع على GitHub Pages).
- لا حاجة لتكوين لغة المصدر، سيكتشف ZEN ذلك تلقائياً.

```bash
npx zengen@latest build --lang zh-Hans --lang en-US
```

**عرض المزيد من المعلمات أو المساعدة**:

```bash
npx zengen@latest
```
