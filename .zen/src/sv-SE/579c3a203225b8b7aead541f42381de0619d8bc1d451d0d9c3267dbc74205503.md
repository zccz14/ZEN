---
'title': 'KaTeX-formelvisningstest'
'summary': 'Detta dokument är ett exempel för att testa KaTeX-formelvisning, innehållande flera inline- och blockmatematiska formler som E=mc^2, integraler, derivator, serier, styckvisa funktioner, tabeller, matriser etc., med syfte att verifiera korrekt rendering av formler.'
'tags':
  - 'KaTeX'
  - 'formeltest'
  - 'matematiska formler'
  - 'LaTeX'
  - 'visningseffekt'
  - 'dokumentexempel'
'inferred_lang': 'zh-Hans'
---

# KaTeX-formelvisningstest

Detta är ett test av en inline-formel: $ E = mc^2 $.

Detta är ett test av en blockformel:

$$
\int_a^b f(x) \, dx = F(b) - F(a)
$$

Detta är ytterligare ett test av en blockformel:

$$
\frac{d}{dx}e^x = e^x
$$

Detta är ett test av en komplex formel:

$$
\sum_{n=1}^{\infty} \frac{1}{n^2} = \frac{\pi^2}{6}
$$

Test av formel med cases:

$$
f(x) =
\begin{cases}
x^2 & \text{om } x \geq 0 \\
-x & \text{om } x < 0
\end{cases}
$$

Tabellformel:

$$
\begin{array}{c|c|c}
a & b & c \\
\hline
1 & 2 & 3 \\
4 & 5 & 6 \\
\end{array}
$$

Matrisformel:

$$
\begin{pmatrix}
1 & 2 \\
3 & 4
\end{pmatrix}
$$

Test av formel med text:

$$
E = mc^2 \quad \text{där } m \text{ är massa och } c \text{ är ljusets hastighet}
$$

Generaliserad Kelly-formel:

$$
f^* = \argmax_f \sum_{i \in X} P_i \ln (1 + f \cdot R_i)
$$

Se till att alla ovanstående formler visas korrekt.
